var table = $("table#expenses");
var form = $("form");

<% if @expense.errors.any? %>
  form.find(".errors").replaceWith("<%= j render "shared/errors", object: @expense %>");
<% else %>
  form.replaceWith("<%= j render "form", expense: Expense.new %>");
  table.DataTable().ajax.reload();
<% end %>
